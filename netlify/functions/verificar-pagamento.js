exports.handler=async(event)=>{try{const accessToken=process.env.MP_ACCESS_TOKEN;if(!accessToken)return response(500,{error:"MP_ACCESS_TOKEN não configurado."});const paymentId=event.queryStringParameters?.paymentId;if(!paymentId)return response(400,{error:"paymentId obrigatório."});const mp=await fetch(`https://api.mercadopago.com/v1/payments/${paymentId}`,{headers:{Authorization:`Bearer ${accessToken}`}});const data=await mp.json();if(!mp.ok)return response(mp.status,{error:"Erro ao consultar pagamento.",details:data});return response(200,{paymentId:data.id,status:data.status,statusDetail:data.status_detail,productId:data.metadata?.product_id,productType:data.metadata?.product_type,items:data.metadata?.items})}catch(e){return response(500,{error:e.message})}};function response(statusCode,body){return{statusCode,headers:{"Content-Type":"application/json","Access-Control-Allow-Origin":"*"},body:JSON.stringify(body)}}